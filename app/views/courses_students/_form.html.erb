<%= form_for(@courses_student) do |f| %>
  <% if @courses_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@courses_student.errors.count, "error") %> prohibited this courses_student from being saved:</h2>

      <ul>
      <% @courses_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.number_field :course_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
